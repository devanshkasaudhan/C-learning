#include<iostream>
#include<vector>
using namespace std;

int main(){
    vector<int> arr;
    arr.push_back(3);
    arr.push_back(9);
    arr.push_back(2);
    arr.push_back(7);
    arr.push_back(5);
    for (int i = 0; i < arr.size(); i++)
    {
        if (arr[i])
        {
            /* code */
        }
        
    }
    
    return 0;
}